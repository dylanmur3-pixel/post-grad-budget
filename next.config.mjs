/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow images from any HTTPS source (useful for future avatars/logos)
  images: {
    remotePatterns: [],
  },
}

export default nextConfig;
